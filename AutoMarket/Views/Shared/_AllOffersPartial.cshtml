@model ListMyOffersViewModel
@using System.Security.Claims

<div class="row mt-2 mb-4 justify-content-center">
    <section class="tiles">
        @foreach (var offer in @Model.Offers)
        {
            @*<div class="card col-6 col-lg-3 game-card mx-2 my-2">
            <img src="@offer.Image"
                 class="card-img-top img-fluid card-image rounded" data-toggle="tooltip"
                 data-placement="bottom" title="">*@
            @if (offer.Make != null)
            {
        <article>
            <span class="image">
                <img src="@offer.Image" alt="" />
            </span>
            <a href="/Offers/VehicleDetails?offerId=@offer.Id">
                <h2>@offer.Make @offer.Model </h2>

                <p><strong>@offer.Price &euro;</strong></p>

                <p>
                    <i class="fas fa-gas-pump"></i> @offer.EngineType &nbsp;&nbsp;&nbsp;&nbsp;
                    <i class="far fa-calendar-alt"></i> @offer.ManufactoringYear&nbsp;&nbsp;&nbsp;&nbsp;
                    <i class="fas fa-palette"></i> @offer.Color
                </p>
            </a>
        </article>
            
                @*<div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item text-center">Brand: @offer.Make </li>
                        <li class="bg-secondary col-sm">Model: @offer.Model</li>
                        <li class="bg-secondary col-sm">Color: @offer.Color</li>
                        <li class="bg-secondary col-sm">Price: @offer.Price &euro;</li>
                        <li class="bg-secondary col-sm">Created on: @offer.CreatedOn</li>
                    </ul>
                    <hr />
                    <a href="/Offers/VehicleDetails?offerId=@offer.Id" class="btn btn-block btn-primary"><i class="fas fa-info-circle"></i> Details</a>
                    @if (User.FindFirst(ClaimTypes.NameIdentifier).Value == offer.OwnerId || User.IsInRole("Admin"))
                    {
                        <a href="/Offers/EditVehicle?offerId=@offer.Id" class="btn btn-block btn-warning"><i class="far fa-edit"></i> Edit</a>
                        <a href="/Offers/Delete?offerId=@offer.Id" class="btn btn-block btn-danger"><i class="fas fa-trash-alt"></i> Delete</a>
                    }
                </div>*@
            }
            else
            {
                <article>
                    <span class="image">
                        <img src="@offer.Image" alt="" />
                    </span>
                    <a href="/Offers/PartDetails?offerId=@offer.Id">
                        <h2>@offer.Title </h2>

                        <p><strong>@offer.Price &euro;</strong></p>

                        <p>
                            <i class="fa fa-info-circle"></i> @offer.Status &nbsp;&nbsp;&nbsp;&nbsp;
                        </p>
                    </a>
                </article>
                @*<div class="card-body">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item text-center">Name: @offer.Name </li>
                    <li class="bg-secondary col-sm">Condition: @offer.Status</li>
                    <li class="bg-secondary col-sm">Price: @offer.Price &euro;</li>
                    <li class="bg-secondary col-sm">Created on: @offer.CreatedOn</li>
                </ul>
                <hr />*@
                @*<a href="/Offers/PartDetails?offerId=@offer.Id" class="btn btn-block btn-primary"><i class="fas fa-info-circle"></i> Details</a>
                @if (User.FindFirst(ClaimTypes.NameIdentifier).Value == offer.OwnerId || User.IsInRole("Admin"))
                {
                    <a href="/Offers/EditPart?offerId=@offer.Id" class="btn btn-block btn-warning"><i class="far fa-edit"></i> Edit</a>
                    <a href="/Offers/Delete?offerId=@offer.Id" class="btn btn-block btn-danger"><i class="fas fa-trash-alt"></i> Delete</a>
                }*@
                @*</div>*@
            }
            @*</div>*@
        }
    </section>
</div>