@model SearchVehicleViewModel

@{
    ViewData["Title"] = "Search in cars offers";
}

<h3 class="text-center">Search in cars offers by 'Brand' and 'Model'</h3>

<form method="get">
    <div class="row">
        <div class="col-md-6 col-sm-12 col-xl-9">
            <div>
                <label asp-for="Make"></label>
                <select class="form-group" name="Make">
                    <option value="">All</option>
                    @foreach (var make in Model.Makes)
                    {
                        <option value="@make">@make</option>
                    }
                </select>
            </div>
            <div>
                <label asp-for="VehicleModel"></label>
                <input class="form-group" type="text" asp-for="VehicleModel" value="" id="VehicleModel" placeholder="Enter a model" />
            </div>
            <div class="form-group">
                <i class="fas fa-search icon-large"></i>
                <input type="submit" class="btn btn-primary" value="Search" />
            </div>
        </div>
    </div>
</form>
<hr />
<h2 class="text-center">Found offers</h2>
<partial name="_VehicleAllPartial" model="@Model" />
<hr />
<partial name="_PagingPartial" model="@Model" />
